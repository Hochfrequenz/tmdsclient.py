name: "Linting"

on:
  push:
    branches: [main]
  pull_request: {}
jobs:
  pylint:
    name: Python Code Quality and Lint
    runs-on: ubuntu-latest
    strategy:
      matrix:
        linter-env: ["linting", "type_check", "spell_check"]
    steps:
      - uses: actions/checkout@v7
      - name: Install uv
        uses: astral-sh/setup-uv@v9.0.0
        with:
          python-version: "3.14"
      - name: Run linting
        if: matrix.linter-env == 'linting'
        run: |
          uv run --group linting pylint tmdsclient
          uv run --group linting pylint unittests --rcfile=unittests/.pylintrc
      - name: Run type_check
        if: matrix.linter-env == 'type_check'
        run: |
          uv run --group type_check mypy --show-error-codes src/tmdsclient --strict
          uv run --group type_check mypy --show-error-codes unittests
      - name: Run spell_check
        if: matrix.linter-env == 'spell_check'
        run: |
          uv run --group spell_check codespell --ignore-words=domain-specific-terms.txt src
          uv run --group spell_check codespell --ignore-words=domain-specific-terms.txt README.md
          uv run --group spell_check codespell --ignore-words=domain-specific-terms.txt unittests
